version: '3.8'

services:
  gotenberg:
    image: gotenberg/gotenberg:8
    container_name: gotenberg
    ports:
      - "3001:3000"
    restart: unless-stopped
    environment:
      - LOG_LEVEL=info
